server {
	listen 80 default_sever;

	location / {
		return 404;
	}

	location ~* ^*\.\w$ {
		root ~/box/web/public;
	}
	
	location ~* ^(/*/){1}$ {
		return 404;
	}

	location ^~ /uploads/ {
		location ~* ^(/*/){1}$ {
			return 404;
		}
		alias /uploads;
	}
}
